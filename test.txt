Linux 批量杀掉进程 kill -9

	查看进程  ps 命令用于查看当前正在运行的进程， grep是搜索 ，例如：查看php应用进程
	ps -ef | grep php
	
	按用户 比如杀掉root用户创建的进程
	pgrep -u root | sudo xargs kill -9
	
	按程序类型  比如杀掉php的进程
	pgrep -f php | sudo xargs kill -9 